diff --git a/frontend/src/components/Certifications.jsx b/frontend/src/components/Certifications.jsx
index 0022483..312e754 100644
--- a/frontend/src/components/Certifications.jsx
+++ b/frontend/src/components/Certifications.jsx
@@ -47,7 +47,7 @@ const Certifications = () => {
             const Icon = iconMap[cert.icon];
             return (
               <div
-                key={i}
+                key={cert.name}
                 className={`group relative p-6 rounded-2xl border border-[#E2E8F0] bg-white/50 hover:border-[#2B6CB0]/30 text-center transition-all duration-500 hover:-translate-y-1 hover:shadow-xl hover:shadow-[#2B6CB0]/5 ${
                   isVisible
                     ? 'opacity-100 translate-y-0'
@@ -86,7 +86,7 @@ const Certifications = () => {
           <div className="grid grid-cols-1 md:grid-cols-3 gap-5">
             {education.map((edu, i) => (
               <div
-                key={i}
+                key={`${edu.school}-${edu.degree}`}
                 className={`p-6 rounded-2xl border border-[#E2E8F0] bg-white/30 hover:border-[#2B6CB0]/20 hover:bg-[#2B6CB0]/5 transition-all duration-500 ${
                   isVisible
                     ? 'opacity-100 translate-y-0'
diff --git a/frontend/src/components/Contact.jsx b/frontend/src/components/Contact.jsx
index c9b6820..f29f7af 100644
--- a/frontend/src/components/Contact.jsx
+++ b/frontend/src/components/Contact.jsx
@@ -100,7 +100,7 @@ const Contact = () => {
               }`}
             >
               <div className="space-y-4">
-                {contactCards.map((card, i) => {
+                {contactCards.map((card) => {
                   const Icon = card.icon;
                   const isLink = !!card.href;
                   const Tag = isLink ? 'a' : 'div';
@@ -118,7 +118,7 @@ const Contact = () => {
 
                   return (
                     <Tag
-                      key={i}
+                      key={card.label}
                       {...linkProps}
                       className="flex items-center gap-4 p-5 rounded-xl border border-[#E2E8F0] bg-white/50 hover:border-[#2B6CB0]/20 hover:bg-[#2B6CB0]/5 transition-all duration-300 group cursor-pointer"
                     >
@@ -145,9 +145,9 @@ const Contact = () => {
                   <h3 className="text-[#1A202C] font-semibold">Languages</h3>
                 </div>
                 <div className="flex flex-wrap gap-3">
-                  {personalInfo.languages.map((l, i) => (
+                  {personalInfo.languages.map((l) => (
                     <span
-                      key={i}
+                      key={l.lang}
                       className="px-4 py-2 rounded-lg bg-[#2B6CB0]/10 text-sm text-[#2B6CB0] font-medium border border-[#2B6CB0]/20"
                     >
                       {l.lang} — {l.level}
diff --git a/frontend/src/components/CookieConsent.jsx b/frontend/src/components/CookieConsent.jsx
index 1a64858..d9e5682 100644
--- a/frontend/src/components/CookieConsent.jsx
+++ b/frontend/src/components/CookieConsent.jsx
@@ -11,6 +11,8 @@ const CookieConsent = () => {
       const timer = setTimeout(() => setVisible(true), 1500);
       return () => clearTimeout(timer);
     }
+    return undefined;
+    // setVisible is a stable React setter; localStorage is a global.
   }, []);
 
   const handleAccept = () => {
diff --git a/frontend/src/components/Expertise.jsx b/frontend/src/components/Expertise.jsx
index 77305c0..a13af34 100644
--- a/frontend/src/components/Expertise.jsx
+++ b/frontend/src/components/Expertise.jsx
@@ -46,8 +46,8 @@ const SkillCard = ({ skill, index, isVisible }) => {
           {skill.name}
         </h3>
         <ul className="space-y-1.5">
-          {skill.items.map((item, j) => (
-            <li key={j} className="text-sm text-[#64748B] flex items-center gap-2">
+          {skill.items.map((item) => (
+            <li key={item} className="text-sm text-[#64748B] flex items-center gap-2">
               <span className="w-1 h-1 rounded-full bg-[#2B6CB0]/50 flex-shrink-0" />
               {item}
             </li>
@@ -87,7 +87,7 @@ const Expertise = () => {
         {/* Skills Grid */}
         <div className="mt-16 grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-5 perspective-container">
           {expertise.map((skill, i) => (
-            <SkillCard key={i} skill={skill} index={i} isVisible={isVisible} />
+            <SkillCard key={skill.name} skill={skill} index={i} isVisible={isVisible} />
           ))}
         </div>
       </div>
diff --git a/frontend/src/components/FAQ.jsx b/frontend/src/components/FAQ.jsx
index 216f49a..a4bde5a 100644
--- a/frontend/src/components/FAQ.jsx
+++ b/frontend/src/components/FAQ.jsx
@@ -96,7 +96,7 @@ const FAQ = () => {
         <div className="mt-12 space-y-4">
           {faqData.map((item, i) => (
             <FAQItem
-              key={i}
+              key={item.question}
               item={item}
               index={i}
               isOpen={openIndex === i}
diff --git a/frontend/src/components/Header.jsx b/frontend/src/components/Header.jsx
index e48677c..6e306c6 100644
--- a/frontend/src/components/Header.jsx
+++ b/frontend/src/components/Header.jsx
@@ -10,6 +10,7 @@ const Header = () => {
     const handleScroll = () => setScrolled(window.scrollY > 50);
     window.addEventListener('scroll', handleScroll);
     return () => window.removeEventListener('scroll', handleScroll);
+    // setScrolled is a stable React setter; no other external deps.
   }, []);
 
   const handleNavClick = (e, href) => {
diff --git a/frontend/src/components/Hero.jsx b/frontend/src/components/Hero.jsx
index 491c97b..20312c8 100644
--- a/frontend/src/components/Hero.jsx
+++ b/frontend/src/components/Hero.jsx
@@ -164,8 +164,8 @@ const Hero = () => {
               : 'opacity-0 translate-y-10'
           }`}
         >
-          {stats.map((stat, i) => (
-            <StatCounter key={i} {...stat} />
+          {stats.map((stat) => (
+            <StatCounter key={stat.label} {...stat} />
           ))}
         </div>
 
diff --git a/frontend/src/components/ImpactMetrics.jsx b/frontend/src/components/ImpactMetrics.jsx
index ca7ff26..4d58a54 100644
--- a/frontend/src/components/ImpactMetrics.jsx
+++ b/frontend/src/components/ImpactMetrics.jsx
@@ -54,7 +54,7 @@ const ImpactMetrics = () => {
           <div className="mt-16 grid grid-cols-2 md:grid-cols-4 gap-5 perspective-container">
             {metrics.map((item, i) => (
               <MetricCard
-                key={i}
+                key={item.label}
                 item={item}
                 index={i}
                 isVisible={isVisible}
diff --git a/frontend/src/components/SEO.jsx b/frontend/src/components/SEO.jsx
index 8b13dfe..fb7350b 100644
--- a/frontend/src/components/SEO.jsx
+++ b/frontend/src/components/SEO.jsx
@@ -1,8 +1,8 @@
 // SEO component — React 19 compatible.
-// Uses useEffect to imperatively manage <title>, meta tags, canonical link,
-// and JSON-LD scripts in document.head. Cleans up on route change.
+// Imperatively manages <title>, meta tags, canonical link, and JSON-LD scripts
+// in document.head via useEffect. Cleans up on route change.
 
-import { useEffect } from 'react';
+import { useEffect, useMemo } from 'react';
 
 const SITE_URL = 'https://karimchaouki.com';
 const DEFAULT_IMAGE = `${SITE_URL}/images/karim-chaouki.jpg`;
@@ -27,6 +27,45 @@ const setLink = (rel, href) => {
   el.setAttribute('href', href);
 };
 
+const applyHead = ({ fullTitle, description, canonicalUrl, ogImage, ogType, schema }) => {
+  document.title = fullTitle;
+  setMeta('meta[name="description"]', 'name', 'description', description);
+  setLink('canonical', canonicalUrl);
+
+  setMeta('meta[property="og:type"]', 'property', 'og:type', ogType);
+  setMeta('meta[property="og:url"]', 'property', 'og:url', canonicalUrl);
+  setMeta('meta[property="og:title"]', 'property', 'og:title', fullTitle);
+  setMeta('meta[property="og:description"]', 'property', 'og:description', description);
+  setMeta('meta[property="og:image"]', 'property', 'og:image', ogImage);
+  setMeta('meta[property="og:image:width"]', 'property', 'og:image:width', '1200');
+  setMeta('meta[property="og:image:height"]', 'property', 'og:image:height', '630');
+  setMeta('meta[property="og:site_name"]', 'property', 'og:site_name', 'Karim Chaouki');
+  setMeta('meta[property="og:locale"]', 'property', 'og:locale', 'en_CA');
+
+  setMeta('meta[name="twitter:card"]', 'name', 'twitter:card', 'summary_large_image');
+  setMeta('meta[name="twitter:title"]', 'name', 'twitter:title', fullTitle);
+  setMeta('meta[name="twitter:description"]', 'name', 'twitter:description', description);
+  setMeta('meta[name="twitter:image"]', 'name', 'twitter:image', ogImage);
+
+  document.head
+    .querySelectorAll('script[data-seo-jsonld="1"]')
+    .forEach((n) => n.remove());
+
+  const created = [];
+  if (schema) {
+    const blocks = Array.isArray(schema) ? schema : [schema];
+    blocks.forEach((obj) => {
+      const s = document.createElement('script');
+      s.type = 'application/ld+json';
+      s.setAttribute('data-seo-jsonld', '1');
+      s.text = JSON.stringify(obj);
+      document.head.appendChild(s);
+      created.push(s);
+    });
+  }
+  return created;
+};
+
 export default function SEO({
   title,
   description,
@@ -35,52 +74,24 @@ export default function SEO({
   ogType = 'website',
   schema = null,
 }) {
-  const fullTitle = title.includes('Karim Chaouki') ? title : `${title} | Karim Chaouki`;
-  const canonicalUrl = canonical ? `${SITE_URL}${canonical}` : SITE_URL;
+  const config = useMemo(
+    () => ({
+      fullTitle: title.includes('Karim Chaouki') ? title : `${title} | Karim Chaouki`,
+      description,
+      canonicalUrl: canonical ? `${SITE_URL}${canonical}` : SITE_URL,
+      ogImage,
+      ogType,
+      schema,
+    }),
+    [title, description, canonical, ogImage, ogType, schema],
+  );
 
   useEffect(() => {
-    document.title = fullTitle;
-
-    setMeta('meta[name="description"]', 'name', 'description', description);
-    setLink('canonical', canonicalUrl);
-
-    setMeta('meta[property="og:type"]', 'property', 'og:type', ogType);
-    setMeta('meta[property="og:url"]', 'property', 'og:url', canonicalUrl);
-    setMeta('meta[property="og:title"]', 'property', 'og:title', fullTitle);
-    setMeta('meta[property="og:description"]', 'property', 'og:description', description);
-    setMeta('meta[property="og:image"]', 'property', 'og:image', ogImage);
-    setMeta('meta[property="og:image:width"]', 'property', 'og:image:width', '1200');
-    setMeta('meta[property="og:image:height"]', 'property', 'og:image:height', '630');
-    setMeta('meta[property="og:site_name"]', 'property', 'og:site_name', 'Karim Chaouki');
-    setMeta('meta[property="og:locale"]', 'property', 'og:locale', 'en_CA');
-
-    setMeta('meta[name="twitter:card"]', 'name', 'twitter:card', 'summary_large_image');
-    setMeta('meta[name="twitter:title"]', 'name', 'twitter:title', fullTitle);
-    setMeta('meta[name="twitter:description"]', 'name', 'twitter:description', description);
-    setMeta('meta[name="twitter:image"]', 'name', 'twitter:image', ogImage);
-
-    // Remove any previous route-scoped JSON-LD blocks, then add the new ones.
-    document.head
-      .querySelectorAll('script[data-seo-jsonld="1"]')
-      .forEach((n) => n.remove());
-
-    const created = [];
-    if (schema) {
-      const blocks = Array.isArray(schema) ? schema : [schema];
-      blocks.forEach((obj) => {
-        const s = document.createElement('script');
-        s.type = 'application/ld+json';
-        s.setAttribute('data-seo-jsonld', '1');
-        s.text = JSON.stringify(obj);
-        document.head.appendChild(s);
-        created.push(s);
-      });
-    }
-
+    const created = applyHead(config);
     return () => {
       created.forEach((n) => n.parentNode && n.parentNode.removeChild(n));
     };
-  }, [fullTitle, description, canonicalUrl, ogImage, ogType, schema]);
+  }, [config]);
 
   return null;
 }
diff --git a/frontend/src/components/Testimonials.jsx b/frontend/src/components/Testimonials.jsx
index 4d4ef36..d50cee8 100644
--- a/frontend/src/components/Testimonials.jsx
+++ b/frontend/src/components/Testimonials.jsx
@@ -68,7 +68,7 @@ const Testimonials = () => {
         <div className="mt-16 grid grid-cols-1 md:grid-cols-2 gap-6 perspective-container">
           {displayed.map((item, i) => (
             <TestimonialCard
-              key={i}
+              key={`${item.name}-${item.company}`}
               item={item}
               index={i}
               isVisible={isVisible}
diff --git a/frontend/src/components/Timeline.jsx b/frontend/src/components/Timeline.jsx
index f33f794..42fe2a6 100644
--- a/frontend/src/components/Timeline.jsx
+++ b/frontend/src/components/Timeline.jsx
@@ -31,8 +31,8 @@ const TimelineCard = ({ item }) => {
           {item.company} &middot; {item.subtitle}
         </p>
         <ul className="mt-4 space-y-2">
-          {item.achievements.map((a, j) => (
-            <li key={j} className="flex items-start gap-2 text-sm text-[#64748B]">
+          {item.achievements.map((a) => (
+            <li key={a} className="flex items-start gap-2 text-sm text-[#64748B]">
               <ChevronRight
                 size={14}
                 className="text-[#2B6CB0]/50 mt-0.5 flex-shrink-0"
@@ -164,7 +164,7 @@ const Timeline = () => {
         <div className="relative">
           {experience.map((item, i) => (
             <TimelineEntry
-              key={i}
+              key={`${item.company}-${item.period}-${item.role}`}
               item={item}
               index={i}
               isLast={i === experience.length - 1}
diff --git a/frontend/src/components/ValueProposition.jsx b/frontend/src/components/ValueProposition.jsx
index 3b6e0fc..98fb392 100644
--- a/frontend/src/components/ValueProposition.jsx
+++ b/frontend/src/components/ValueProposition.jsx
@@ -64,7 +64,7 @@ const ValueProposition = () => {
           {differentiators.map((item, i) => {
             const Icon = iconMap[item.icon];
             return (
-              <TiltCard key={i} index={i} isVisible={isVisible}>
+              <TiltCard key={item.title} index={i} isVisible={isVisible}>
                 <div className="w-12 h-12 rounded-xl bg-[#2B6CB0]/10 flex items-center justify-center mb-5 group-hover:bg-[#2B6CB0]/20 transition-colors duration-300">
                   <Icon size={24} className="text-[#2B6CB0]" />
                 </div>
diff --git a/frontend/src/hooks/use-toast.js b/frontend/src/hooks/use-toast.js
index 03accc0..2c7fb2d 100644
--- a/frontend/src/hooks/use-toast.js
+++ b/frontend/src/hooks/use-toast.js
@@ -143,7 +143,8 @@ function useToast() {
         listeners.splice(index, 1)
       }
     };
-  }, [state])
+    // eslint-disable-next-line react-hooks/exhaustive-deps
+  }, [])
 
   return {
     ...state,
